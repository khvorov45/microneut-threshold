# Model for titre threshold

model {
    for (i in 1:n) {
        logtitre_point[i] ~ dnorm(mu[i], 1 / sigma^2) T(logtitre_low[i], logtitre_high[i])

        mu[i] ~ beta_0 + beta_1 * status[i]
    }

    sigma ~ dexp(0.1)
    beta_0 ~ dnorm(1, 1 / 1^2)
    beta_1 ~ dnorm(3, 1 / 1.5^2)
}
